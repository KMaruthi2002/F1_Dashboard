import RaceCenter from '@/components/RaceCenter';

export const metadata = {
  title: 'RACE CENTER — APEX // TELEMETRY',
  description: 'Live track map, positions, lap times, sectors, tires and onboard telemetry.',
};

export default function LivePage() {
  return <RaceCenter />;
}
