import Paddock from '@/components/Paddock';

export const metadata = {
  title: 'THE PADDOCK — APEX // TELEMETRY',
  description: 'Drag your podium picks, call pole and the sprint, earn Paddock Points against real FIA results, and climb the leaderboard.',
};

export default function PaddockPage() {
  return <Paddock />;
}
